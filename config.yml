app:
  name: doc_query
  env: ${APP_ENV}
  host: ${APP_HOST}
  port: ${APP_PORT}

database:
  host: ${POSTGRES_HOST}
  port: ${POSTGRES_PORT}
  name: ${POSTGRES_DB}
  user: ${POSTGRES_USER}
  password: ${POSTGRES_PASSWORD}

redis:
  host: ${REDIS_HOST}
  port: ${REDIS_PORT}

logging:
  level: ${LOG_LEVEL}